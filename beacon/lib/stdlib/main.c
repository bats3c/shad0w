#include <windows.h>

char* listdirs(LPCSTR szDir);
char* readfile(char* szFile);
char* makedirectory(char* szDirName);
char* removefile(char* szFileName);
char* getdir();

int main()
{
    // printf(listdirs("C:\\windows\\system32"));
    // printf(readfile("C:\\Users\\thejoker\\Desktop\\user.txt"));
    // printf(makedirectory("C:\\Users\\thejoker\\Desktop\\secret"));
    // printf(removefile("C:\\Users\\thejoker\\Desktop\\secret"));
    // printf(getdir());
}